'use strict';

describe('docScrypt.editor module', function() {

  beforeEach(module('docScrypt.editor'));

  describe('editor controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var editorCtrl = $controller('editorCtrl');
      expect(editorCtrl).toBeDefined();
    }));

  });
});